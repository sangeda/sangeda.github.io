name: Retired personal DCEPD refresh

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: DCEPD moved to standalone repository
        run: |
          echo "Personal-site DCEPD refresh retired."
          echo "Current site: https://muhas-dcepd.github.io/"
